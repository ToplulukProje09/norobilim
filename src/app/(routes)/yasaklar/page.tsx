import React from "react";
import WrongWords from "./_components/WrongWords";

const page = () => {
  return (
    <div>
      <WrongWords /> {/* client component */}
    </div>
  );
};

export default page;
