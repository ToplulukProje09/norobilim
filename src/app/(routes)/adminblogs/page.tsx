import React from "react";
import BlogList from "./_components/BlogList";

const page = () => {
  return (
    <div>
      <BlogList /> {/* client component */}
    </div>
  );
};

export default page;
