import AdminPodcast from "../_components/AdminPodcast";

const AddPodcastPage = () => {
  return (
    <div>
      {/* This is a Client Component. It's empty initially and manages its own state for the form.
        We'll create this component in the next step.
      */}
      <AdminPodcast />
    </div>
  );
};

export default AddPodcastPage;
